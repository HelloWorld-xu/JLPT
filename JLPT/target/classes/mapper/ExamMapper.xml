<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xuyifei.jlpt.mapper.ExamMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO exam (
            level, year, month,

            language_raw_score,
            reading_raw_score,
            listening_raw_score,

            language_scaled_score,
            reading_scaled_score,
            listening_scaled_score,

            total_scaled_score,
            pass
        ) VALUES (
                     #{level}, #{year}, #{month},

                     #{languageRawScore},
                     #{readingRawScore},
                     #{listeningRawScore},

                     #{languageScaledScore},
                     #{readingScaledScore},
                     #{listeningScaledScore},

                     #{totalScaledScore},
                     #{pass}
                 )
    </insert>

    <select id="selectById" resultType="com.xuyifei.jlpt.entity.Exam">
        SELECT * FROM exam WHERE id = #{id}
    </select>

    <update id="updateById" parameterType="com.xuyifei.jlpt.entity.Exam">
        update exam
        set language_raw_score = #{languageRawScore},
            reading_raw_score = #{readingRawScore},
            listening_raw_score = #{listeningRawScore},
            language_scaled_score = #{languageScaledScore},
            reading_scaled_score = #{readingScaledScore},
            listening_scaled_score = #{listeningScaledScore},
            total_scaled_score = #{totalScaledScore},
            pass = #{pass}
        where id = #{id}
    </update>
    <delete id="deleteById">
        DELETE FROM exam
        WHERE id = #{id}
    </delete>

    <select id="selectAll" resultType="com.xuyifei.jlpt.entity.Exam">
        SELECT * FROM exam ORDER BY id DESC
    </select>

</mapper>